/* tslint:disable:no-unused-variable */
import { FormatDateTimePipe } from './FormatDateTime.pipe';

describe('Pipe: FormatDateTimee', () => {
  it('create an instance', () => {
    let pipe = new FormatDateTimePipe('pt-br');
    expect(pipe).toBeTruthy();
  });
});
